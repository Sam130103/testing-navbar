'use client';

import EventsCard from '../../components/events/MapEvent';
import { Navbar } from '../../components';

const Events = () => (
  <div>
    <Navbar />
    <EventsCard />
  </div>
);
export default Events;
